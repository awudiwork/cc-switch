好的，进程已终止。现在您可以重新启动：
node test-vite.mjs
等待看到 Vite 启动成功后，在另一个终端运行：
cd src-tauri
pnpm exec tauri dev
##💡 提示
如果您经常遇到端口占用问题，可以在启动前先检查并清理：
# 检查端口
netstat -ano | findstr ":3000"
# 如果有占用，杀掉进程
taskkill /F /PID <进程ID>
